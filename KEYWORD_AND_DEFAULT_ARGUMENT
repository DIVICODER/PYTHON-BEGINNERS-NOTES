 ##Keyword Arguments (or Named Arguments):


#Its a bit similar to dictionary-because both have a key-value pair relationship. 
    #For instance, name="Alice" means the "key" is name and the "value" is "Alice".

'''
    These are arguments you pass to a function by name rather than just by their position.
    When you call a function, you specify the name of the parameter followed by the = sign and give it a value.'''

def greet(name, message):
    print(f"{message}, {name}!")

greet(name="Alice", message="Hello")  # Keyword arguments -- easy to understand 


##Default Arguments:  don’t provide a value when calling the function

def greet(name, message="Hello"):
    print(f"{message}, {name}!")

greet("Bob")  # Output: Hello, Bob!
greet("Bob", "Good morning")  # Output: Good morning, Bob!






